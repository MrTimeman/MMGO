---
phase: 02-living-world-activities-and-survival
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/mmgo/events.ex
  - lib/mmgo/play.ex
  - lib/mmgo_web/live/action_hub_live.ex
  - test/mmgo/events_test.exs
  - test/mmgo_web/live/action_hub_live_test.exs
autonomous: true
requirements: [WORLD-04]
must_haves:
  truths:
    - "A signed-in player sees only the persisted text event and legal actions for their actual current location."
    - "A browser cannot resolve another player's event or choose an arbitrary action route."
  artifacts:
    - path: lib/mmgo/play.ex
      provides: scoped activity hub read/action boundary
      contains: "activity_hub_state"
    - path: lib/mmgo_web/live/action_hub_live.ex
      provides: real location text-event UI
      contains: "activity-hub"
---

<objective>
Replace the design-pass location screen with the real map-first text activity hub.

Purpose: The GDD says arrival presents a description and only valid actions; the current `/event` screen is static demo data.
Output: server-owned event state, trusted action-key routing, and a scoped Russian activity hub.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Harden current-location event lifecycle and add activity facade</name>
  <files>lib/mmgo/events.ex, lib/mmgo/play.ex, test/mmgo/events_test.exs</files>
  <action>Make current event creation concurrency-safe and ensure a stale active event cannot shadow an arrival at a new location. Add `Play.activity_hub_state/1` and `Play.resolve_activity_option/3`; verify that a supplied event ID belongs to the scoped character, remains active, and is at the current location before calling Events. Map only trusted domain `action_key` values to fixed route/availability metadata on the server. Return no activity while the character has an active journey. Do not implement deferred market/base/party systems here.</action>
  <verify>mix test test/mmgo/events_test.exs test/mmgo/play_test.exs</verify>
  <done>Location events are durable, scoped, and safe against stale/foreign IDs.</done>
</task>
<task type="auto">
  <name>Task 2: Implement the real scoped ActionHubLive</name>
  <files>lib/mmgo_web/live/action_hub_live.ex, test/mmgo_web/live/action_hub_live_test.exs</files>
  <action>Replace all module-attribute demo places, fake date, compass selector, and prose-only choices with `Play.activity_hub_state(socket.assigns.current_scope.character)`. Render `<Layouts.app ... current_scope={@current_scope}>`, actual event title/body/location/world date, trusted event options, and stable IDs `activity-hub`, `activity-event-<id>`, and `activity-option-<code>`. Resolve an option through the facade then navigate only to its returned trusted route or show an explicit unavailable/next-phase state. A player in transit navigates to `/travel` instead of getting an arrival screen.</action>
  <verify>mix test test/mmgo_web/live/action_hub_live_test.exs test/mmgo_web/live/map_live_test.exs</verify>
  <done>`/event` is a player-owned text activity hub rather than a location-switching demo.</done>
</task>
</tasks>
