---
phase: 02-living-world-activities-and-survival
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mmgo/travel/clock.ex
  - lib/mmgo/accounts.ex
  - lib/mmgo/play.ex
  - lib/mmgo_web/live/map_live.ex
  - test/mmgo/travel/clock_test.exs
  - test/mmgo/play_test.exs
  - test/mmgo_web/live/map_live_test.exs
autonomous: true
requirements: [WORLD-01, WORLD-02, WORLD-03]
must_haves:
  truths:
    - "A scoped character sees its own realm's map, canonical calendar, location, journey, supplies, and no fixture profile/calendar data."
    - "The map payload includes real nearby active actors and persisted notification delivery history or an honest empty state."
  artifacts:
    - path: lib/mmgo/travel/clock.ex
      provides: canonical 13-month world calendar projection
      contains: "world_time"
    - path: lib/mmgo/play.ex
      provides: scoped world map read model
      contains: "world_hub_state"
    - path: lib/mmgo_web/live/map_live.ex
      provides: scope-realm map mount and real overlay data
      contains: "world_hub_state"
---

<objective>
Make map state canonical, scoped, and truthful before adding commands.

Purpose: The map is the player's primary interface; it must no longer use a default realm or hard-coded date/profile/notification data.
Output: canonical clock, `Play.world_hub_state/1`, nearby-character read, and a scope-realm MapLive with stable map overlay IDs.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add deterministic canonical world calendar and scoped world read model</name>
  <files>lib/mmgo/travel/clock.ex, lib/mmgo/accounts.ex, lib/mmgo/play.ex, test/mmgo/travel/clock_test.exs, test/mmgo/play_test.exs</files>
  <action>Extend `MMGO.Travel.Clock` with a server-time `world_time/1` projection using the GDD's 364-day year, 13 months of 28 days, and four three-month seasons. Provide a deterministic epoch/year configuration or options for tests. Add an Accounts query for active characters at a scoped realm/location, excluding the caller and travellers. Add `Play.world_hub_state/1` that accepts only a character/ID already authorized by a caller and composes its own realm, state/routes/journey, calendar, survival summary, persisted notifications, nearby actors, and open overworld encounters. Keep domain writes out of this read model.</action>
  <verify>mix test test/mmgo/travel/clock_test.exs test/mmgo/play_test.exs</verify>
  <done>One facade gives a signed player the only world state their browser needs, with deterministic calendar boundaries.</done>
</task>
<task type="auto">
  <name>Task 2: Replace map fixtures with the scoped world model</name>
  <files>lib/mmgo_web/live/map_live.ex, test/mmgo_web/live/map_live_test.exs</files>
  <action>Mount MapLive from `socket.assigns.current_scope.character` and `Play.world_hub_state/1`, never `Worlds.get_default_realm!()`. Replace hard-coded calendar, character/profile text, notifications, and `others: []` map payload with the facade values. Add `#map-world-clock`, `#map-current-location`, `#map-activity-link`, `#map-nearby-actors`, `#map-notifications`, and `#map-travel-state`; preserve the existing map renderer and organisation overlay. Remove fake notification mutations rather than pretending they persist. The only current-location action that Phase 2 guarantees is an `Осмотреться` link to `/event`.</action>
  <verify>mix test test/mmgo_web/live/map_live_test.exs test/mmgo_web/play_demo_loop_test.exs</verify>
  <done>The map is a real scoped realm/player read model, including honest empty states.</done>
</task>
</tasks>

<verification>
- [ ] `mix test test/mmgo/travel/clock_test.exs test/mmgo/play_test.exs test/mmgo_web/live/map_live_test.exs`
- [ ] `mix format --check-formatted`
- [ ] `git diff --check`
</verification>
