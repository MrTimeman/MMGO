---
phase: 04-timed-bounded-combat
plan: "03"
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - lib/mmgo/combat/orchestrator.ex
  - lib/mmgo/ai/prompts/combat_turn_prompt.ex
  - lib/mmgo/ai.ex
  - lib/mmgo/combat/narrator.ex
  - README.md
  - test/mmgo/combat/orchestrator_test.exs
  - test/mmgo/combat/narrator_test.exs
autonomous: true
requirements: [MAGIC-03, COMBAT-04, QUALITY-02]
must_haves:
  truths:
    - "Runtime AI receives only an immutable bounded engine envelope and cannot invent mechanics."
    - "Provider/schema failure falls back deterministically and persists useful Russian narration."
    - "Retries reuse the claimed turn input/result rather than double-applying a cast."
  artifacts:
    - path: lib/mmgo/combat/orchestrator.ex
      provides: bounded runtime orchestration/fallback
      contains: "fallback"
---

<objective>
Add the GDD runtime-AI boundary without giving an external model mechanical authority.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build constrained runtime orchestration and fallback</name>
  <files>lib/mmgo/combat/orchestrator.ex, lib/mmgo/ai/prompts/combat_turn_prompt.ex, lib/mmgo/ai.ex, test/mmgo/combat/orchestrator_test.exs</files>
  <action>
Construct a stored turn envelope from normalized action snapshots, allowed state primitives, targets, environment and numeric bounds. Call provider work outside the turn lock with bounded `Task.async_stream/3`; validate returned values back against the envelope. Persist audit linkage and use deterministic spell snapshot/engine fallback on provider, schema, or timeout failure. Never let the provider select raw damage, ownership, rewards, or invalid targets.
  </action>
  <acceptance_criteria>
    - Malformed/out-of-range output is rejected or falls back without corrupting the turn.
    - Multiple caster calls preserve deterministic action ordering.
    - Retry token reuses stored resolution input/results.
  </acceptance_criteria>
  <verify>mix test test/mmgo/combat/orchestrator_test.exs test/mmgo/combat/narrator_test.exs</verify>
</task>
<task type="auto">
  <name>Task 2: Persist narration and reconcile product documentation</name>
  <files>lib/mmgo/combat/narrator.ex, README.md, test/mmgo/combat/narrator_test.exs</files>
  <action>
Persist a Russian narration/fallback artifact for every resolved turn and update README’s runtime-AI description to match the bounded GDD contract. Avoid long transactions around provider I/O.
  </action>
  <acceptance_criteria>
    - Reconnect/spectator reads see persisted narration.
    - README and GDD contract no longer claim contradictory runtime behavior.
  </acceptance_criteria>
  <verify>mix test test/mmgo/combat/narrator_test.exs</verify>
</task>
</tasks>

