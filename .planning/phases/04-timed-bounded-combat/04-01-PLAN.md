---
phase: 04-timed-bounded-combat
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mmgo/combat.ex
  - lib/mmgo/combat/turn.ex
  - lib/mmgo/combat/resolve_turn_worker.ex
  - lib/mmgo/spells/runtime.ex
  - test/mmgo/combat_test.exs
  - test/mmgo/combat/resolve_turn_worker_test.exs
  - test/mmgo/spells/runtime_test.exs
autonomous: true
requirements: [COMBAT-01, COMBAT-03, QUALITY-02]
must_haves:
  truths:
    - "A resolve request is bound to the turn it observed and cannot resolve a newly-created later turn."
    - "Every turn has a persisted authoritative opened/deadline/lock/claim/resolve lifecycle, even when the migration generator is sandbox-blocked."
    - "A due-turn worker converges with manual resolution and duplicate retries without a LiveView timer."
  artifacts:
    - path: lib/mmgo/combat/resolve_turn_worker.ex
      provides: durable deadline completion entrypoint
      contains: "ResolveTurnWorker"
    - path: lib/mmgo/combat.ex
      provides: locked expected-turn resolution and deadline lifecycle
      contains: "deadline_at"
---

<objective>
Make turn timing and resolution durable, locked, retry-safe state.

Purpose: combat currently creates open turns with no deadline and lets a stale resolver use whatever turn happens to be current after it locks.
Output: persisted lifecycle metadata, due-turn worker, expected-turn rejection, deterministic timeout waits, and clean full-suite reliability.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add durable lifecycle data and expected-turn protection</name>
  <files>lib/mmgo/combat.ex, lib/mmgo/combat/turn.ex, lib/mmgo/spells/runtime.ex, test/mmgo/combat_test.exs, test/mmgo/spells/runtime_test.exs</files>
  <action>
Attempt `mix ecto.gen.migration add_combat_turn_deadlines` after reading its help. If the generator remains blocked by the sandbox lock, use the already durable `combat_turns.resolution["lifecycle"]` map rather than hand-writing a migration. Store ISO8601 opened/deadline/locked/claimed/resolved values and a claim token; add `:resolving` to the Ecto turn state. Create turns with participant-scaled deadlines. Make `resolve_turn/1` reject a stale expected turn after locking the combat. Preserve a profile that explicitly declares 100% success as 100% at runtime, while normal spells retain seeded failure behavior.
  </action>
  <acceptance_criteria>
    - `resolve_turn/1` returns `{:error, :turn_closed}` rather than resolving turn N+1 from an N snapshot.
    - Current and next turns expose durable lifecycle metadata.
    - Full-suite killing fixtures no longer fail due to a runtime cap contradicting a 100% profile.
  </acceptance_criteria>
  <verify>mix test test/mmgo/combat_test.exs test/mmgo/spells/runtime_test.exs test/mmgo/dungeon_combat_integration_test.exs</verify>
</task>
<task type="auto">
  <name>Task 2: Schedule and execute deadline resolution idempotently</name>
  <files>lib/mmgo/combat.ex, lib/mmgo/combat/resolve_turn_worker.ex, test/mmgo/combat/resolve_turn_worker_test.exs</files>
  <action>
Schedule an Oban job when a durable turn opens. Its args contain combat and exact turn IDs. On a due turn, lock the combat/turn, materialize only missing participant wait actions, lock the turn, and resolve the same expected turn. An early worker returns a retry/discard-safe result without changing state. Replayed jobs and manually resolved jobs converge to no-op/stale errors rather than duplicate events.
  </action>
  <acceptance_criteria>
    - Due worker adds durable wait actions only after its persisted deadline.
    - An early or duplicate job neither creates a second turn nor duplicates events.
    - Worker tests call `perform/1` directly without sleeps.
  </acceptance_criteria>
  <verify>mix test test/mmgo/combat/resolve_turn_worker_test.exs test/mmgo/combat_test.exs</verify>
</task>
</tasks>

