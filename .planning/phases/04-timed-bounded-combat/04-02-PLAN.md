---
phase: 04-timed-bounded-combat
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mmgo/combat.ex
  - lib/mmgo/combat/action_snapshot.ex
  - lib/mmgo/combat/action.ex
  - lib/mmgo/combat/engine.ex
  - test/mmgo/combat/action_snapshot_test.exs
  - test/mmgo/combat_test.exs
  - test/mmgo/tool_actions_test.exs
autonomous: true
requirements: [COMBAT-01, COMBAT-02, COMBAT-03]
must_haves:
  truths:
    - "Every submitted action is normalized from owned server state before it is stored."
    - "Locked actions cannot be replaced, target a foreign participant, use an uninscribed spell, or consume an unavailable item."
    - "Resolution reads an immutable approved snapshot rather than raw browser payload fields."
  artifacts:
    - path: lib/mmgo/combat/action_snapshot.ex
      provides: action validation/snapshot boundary
      contains: "normalize"
---

<objective>
Close browser-to-engine action authority and make simultaneous turns use immutable legal selections.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Normalize and lock action submissions</name>
  <files>lib/mmgo/combat.ex, lib/mmgo/combat/action_snapshot.ex, lib/mmgo/combat/action.ex, test/mmgo/combat/action_snapshot_test.exs</files>
  <action>
Put submission in one transaction that locks combat/current turn/participant. Validate action type, participant ownership, legal target side/participant, selected grimoire entry/owned spell, normalized one-to-six word incantation for casts, and carried owned tool/item action/quantity/durability. Store only server-approved snapshot data in action payload and permit replace only while open/before deadline. Do not accept raw effect data or arbitrary target sides.
  </action>
  <acceptance_criteria>
    - Foreign participant/spell/item/target and illegal action keys fail before action persistence.
    - A sealed/expired turn cannot be modified.
    - Cast snapshot records normalized incantation and selected permitted base spell; tool snapshot records allowed action/costs.
  </acceptance_criteria>
  <verify>mix test test/mmgo/combat/action_snapshot_test.exs test/mmgo/combat_test.exs test/mmgo/tool_actions_test.exs</verify>
</task>
<task type="auto">
  <name>Task 2: Resolve only normalized snapshots in deterministic turn order</name>
  <files>lib/mmgo/combat/engine.ex, lib/mmgo/combat.ex, test/mmgo/combat_test.exs, test/mmgo/tool_actions_test.exs</files>
  <action>
Make Engine consume the snapshot fields and validate fail-closed if a legacy/raw action cannot be normalized. Retain seeded simultaneous action ordering, shared HP, cooldown/fatigue, state ticks, and magic-zone behavior. Remove any engine path that trusts a client-selected impossible side/target.
  </action>
  <acceptance_criteria>
    - The engine cannot call `Map.update!` with a non-existent client side.
    - An action accepted before another action resolves keeps its legal snapshot despite mutable spell/item data.
  </acceptance_criteria>
  <verify>mix test test/mmgo/combat_test.exs test/mmgo/tool_actions_test.exs test/mmgo/combat/engine_test.exs</verify>
</task>
</tasks>

