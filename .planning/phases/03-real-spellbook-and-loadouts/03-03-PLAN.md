---
phase: 03-real-spellbook-and-loadouts
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - lib/mmgo/play.ex
  - lib/mmgo_web/live/spellbook_live.ex
  - test/mmgo/play_test.exs
  - test/mmgo_web/live/spellbook_live_test.exs
autonomous: true
requirements: [MAGIC-01, MAGIC-02]
must_haves:
  truths:
    - "Every browser composition/loadout command rechecks scoped ownership, permitted composition location, journey state, base spell, and school on the server."
    - "The player operates normal forms/actions with stable IDs; no hook or delayed client state owns a game command."
  artifacts:
    - path: lib/mmgo/play.ex
      provides: scoped spellbook action/read boundary
      contains: "inscribe_spell"
    - path: lib/mmgo_web/live/spellbook_live.ex
      provides: server-rendered composition/loadout UI
      contains: "spell-compose-form"
---

<objective>
Expose the secured compiler and grimoire lifecycle through a scoped, testable Spellbook LiveView.

Purpose: The present spellbook is a Tower-only design pass whose hook triggers a handcrafted `Play.compile_spell/2` and silently selects inscription targets.
Output: real scope-owned spellbook state, Tower-or-active-base eligibility, explicit forms, Russian errors, and focused LiveView coverage.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build scoped spellbook orchestration with location and school policy</name>
  <files>lib/mmgo/play.ex, test/mmgo/play_test.exs</files>
  <read_first>
    - lib/mmgo/play.ex
    - lib/mmgo/bases.ex
    - lib/mmgo/academy.ex
    - lib/mmgo/travel.ex
    - lib/mmgo/spells/compiler.ex
    - lib/mmgo/grimoires.ex
    - test/mmgo/play_test.exs
    - .planning/phases/03-real-spellbook-and-loadouts/03-CONTEXT.md
  </read_first>
  <action>
Refactor `Play.spellbook_state/1`, `compile_spell/2`, `activate_grimoire/2`, and a new explicit `inscribe_spell/3` into one scoped policy boundary. Do not create free grimoires in this phase: books are purchase-only and Phase 5 owns the market transaction. Require the character to be stationary and either at a `:tower` location or owns an active base at the current location before every composition, inscription, or activation command. Return distinct `:travelling` and `:spellbook_location` errors. Derive permitted schools from active wizard specialization when it exists; otherwise permit only schools already represented in the character’s owned library so starter spells remain usable. Require the selected school to be permitted, then call `MMGO.Spells.Compiler.compile_and_store/3`; do not retain deterministic handcrafted effects. Return spellbook state with owned spells, owned grimoires/entries, active loadout, permitted schools, composition location label, and explicit writable grimoire choices. Reject every foreign grimoire/spell/base ID.
  </action>
  <acceptance_criteria>
    - `Play.compile_spell/2` delegates to `Compiler.compile_and_store/3` and does not call `Spells.create_spell/2` directly for browser composition.
    - Wrong-location, travelling, foreign-base, and disallowed-school calls return errors before an AI request is created.
    - Explicit `Play.inscribe_spell(character, grimoire_id, spell_id)` rejects foreign IDs, travel, and wrong-location calls and writes the chosen owned spell only.
  </acceptance_criteria>
  <verify>mix test test/mmgo/play_test.exs</verify>
</task>
<task type="auto">
  <name>Task 2: Replace hook-owned spellbook actions with scoped forms and stable controls</name>
  <files>lib/mmgo_web/live/spellbook_live.ex, test/mmgo_web/live/spellbook_live_test.exs</files>
  <read_first>
    - lib/mmgo_web/live/spellbook_live.ex
    - lib/mmgo/play.ex
    - lib/mmgo_web/live/location_gate.ex
    - .planning/phases/03-real-spellbook-and-loadouts/03-UI-SPEC.md
    - test/mmgo_web/live/spellbook_live_test.exs
    - AGENTS.md
  </read_first>
  <action>
Mount through `Play.spellbook_state/1` instead of a Tower-only mount gate, redirecting `:travelling` to `/travel` and `:spellbook_location` to `/map` with Russian in-world flash copy. Render `<.form id="spell-compose-form">` using `to_form/2` and `<.input>` fields for `base_spell_id`, `school`, and formula; give controls IDs `spell-compose-base`, `spell-compose-school`, `spell-compose-formula`, `spell-compose-submit`, plus result/error IDs from the UI contract. Submit directly to a LiveView event that calls the scoped Play facade, with no `Process.send_after` compile path. Render owned library rows `spell-library-<id>`, grimoire cards `grimoire-<id>`, explicit spell selects and inscription buttons, active-loadout action, and mapped Russian errors/empty states. Do not render or call `grimoire_create`; empty writable-book state explains that new physical books are bought on the market. Retain any existing visual hook only for decoration; it must not submit or construct authoritative composition attrs.
  </action>
  <acceptance_criteria>
    - `SpellbookLive` contains `spell-compose-form` and does not use `Process.send_after` for composition.
    - Scoped LiveView tests use form submit/click against IDs from the UI contract, not raw HTML text.
    - Tests cover Tower/base success, city/travelling redirect, foreign ID rejection, explicit inscription, activation, and a server-rendered validation error.
  </acceptance_criteria>
  <verify>mix test test/mmgo_web/live/spellbook_live_test.exs</verify>
</task>
</tasks>

<verification>
- [ ] `mix test test/mmgo/spells/compiler_test.exs test/mmgo/grimoires_test.exs test/mmgo/play_test.exs test/mmgo_web/live/spellbook_live_test.exs`
- [ ] `mix precommit`
- [ ] `git diff --check`
</verification>
