---
phase: 03-real-spellbook-and-loadouts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mmgo/spells.ex
  - lib/mmgo/spells/compiler.ex
  - lib/mmgo/ai/prompts/spell_compile_prompt.ex
  - test/mmgo/spells/compiler_test.exs
autonomous: true
requirements: [MAGIC-01]
must_haves:
  truths:
    - "A compilation requires an owned same-realm base spell and a normalized one-to-six-word incantation before an AI request is created."
    - "The compiler receives bounded owned base/library context and persists source lineage from the accepted base rather than AI/browser authority."
  artifacts:
    - path: lib/mmgo/spells/compiler.ex
      provides: owned-base compiler boundary
      contains: "base_spell_id"
    - path: lib/mmgo/ai/prompts/spell_compile_prompt.ex
      provides: base and owned-library prompt context
      contains: "base_spell"
---

<objective>
Make the real spell compiler the only persistence path for player composition and close the base-spell ownership gap.

Purpose: The current browser facade bypasses the compiler and treats an optional/foreign base as nil, violating GDD §2.2 and §7.1.
Output: compiler-level owned-base validation, immutable accepted intent fields, and audited prompt context.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Require an owned base spell and bound compiler context</name>
  <files>lib/mmgo/spells.ex, lib/mmgo/spells/compiler.ex, lib/mmgo/ai/prompts/spell_compile_prompt.ex</files>
  <read_first>
    - lib/mmgo/spells/compiler.ex
    - lib/mmgo/spells/incantation.ex
    - lib/mmgo/spells/spell.ex
    - lib/mmgo/ai/prompts/spell_compile_prompt.ex
    - docs/MMGO_GDD.md
    - .planning/phases/03-real-spellbook-and-loadouts/03-CONTEXT.md
  </read_first>
  <action>
Add a non-raising owned/same-realm spell lookup in `MMGO.Spells`. In `Compiler.compile_and_store/3`, normalize formula and school first, then require `base_spell_id` to resolve to a spell whose `creator_character_id` and `realm_id` equal the caller’s. Return a changeset error for blank/missing, unknown, foreign, or cross-realm bases before `AI.compile_spell/2`; do not create an `MMGO.AI.Request` in those cases. Build the compiler prompt with a bounded base summary (id, name, formula, school, description, effects) and an owned-library summary, not only an untrusted ID. Force persisted `formula`, `school`, and `source_spell_id` from normalized request/base values even if the provider returns different values; allow provider output only for validated generated attributes such as name/description/effects/failure profile. Keep existing failed and invalid-output audit behavior.
  </action>
  <acceptance_criteria>
    - `Compiler.compile_and_store/3` rejects a missing or foreign `base_spell_id` before creating `MMGO.AI.Request`.
    - A successful compile has `spell.source_spell_id == base_spell.id`, exact normalized submitted formula, and exact submitted school.
    - `SpellCompilePrompt` serializes `base_spell` and `library` in the user payload.
  </acceptance_criteria>
  <verify>mix test test/mmgo/spells/compiler_test.exs</verify>
</task>
</tasks>

<verification>
- [ ] `mix test test/mmgo/spells/compiler_test.exs`
- [ ] `mix format --check-formatted`
</verification>
