---
phase: 03-real-spellbook-and-loadouts
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mmgo/grimoires.ex
  - test/mmgo/grimoires_test.exs
autonomous: true
requirements: [MAGIC-02]
must_haves:
  truths:
    - "A concurrent inscription cannot overfill or mutate a sealed/active grimoire."
    - "Inscription remains explicit, owned, write-once, duplicate-free, and capacity-bound."
  artifacts:
    - path: lib/mmgo/grimoires.ex
      provides: lock-protected inscription command
      contains: "FOR UPDATE"
---

<objective>
Make grimoire inscription and activation safe for real player loadouts.

Purpose: Capacity and write-once checks currently happen on an unlocked preload, allowing concurrent decisions to observe stale state.
Output: transactionally locked grimoire/entry validation with tests for ownership, capacity, duplicates, sealing, and one active loadout.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Lock writable grimoire state before inscription</name>
  <files>lib/mmgo/grimoires.ex, test/mmgo/grimoires_test.exs</files>
  <read_first>
    - lib/mmgo/grimoires.ex
    - lib/mmgo/grimoires/grimoire.ex
    - lib/mmgo/grimoires/grimoire_entry.ex
    - lib/mmgo/spells/spell.ex
    - test/mmgo/grimoires_test.exs
  </read_first>
  <action>
Wrap `Grimoires.inscribe_spell/3` in a repo transaction. Reload the grimoire row with `FOR UPDATE`, preload its entries/spells after the lock, then validate owner/realm, `:draft` writable status, capacity, and duplicate spell before inserting the explicit supplied spell. Preserve existing error shapes and slot-index ordering. For activation, lock every grimoire owned by the character with `FOR UPDATE` before sealing any previous active book and activating the selected owned book, so concurrent activation attempts serialize around the partial unique active-owner index. Add focused tests for explicit foreign spell rejection, duplicate/capacity rejection, sealed/active write rejection, and active loadout replacement.
  </action>
  <acceptance_criteria>
    - `Grimoires.inscribe_spell/3` contains a `Repo.transaction` and locked grimoire query.
    - Tests show a foreign owner spell, duplicate spell, full grimoire, and sealed/active grimoire each return an error without adding an entry.
    - Activation locks owner grimoires and leaves exactly one active grimoire owned by the character.
  </acceptance_criteria>
  <verify>mix test test/mmgo/grimoires_test.exs</verify>
</task>
</tasks>

<verification>
- [ ] `mix test test/mmgo/grimoires_test.exs`
- [ ] `mix format --check-formatted`
</verification>
