---
phase: 01-secure-player-identity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/config.exs
  - config/runtime.exs
  - config/test.exs
  - lib/mmgo/telegram.ex
  - lib/mmgo/telegram/web_app_auth.ex
  - test/mmgo/telegram/web_app_auth_test.exs
  - test/mmgo/telegram_test.exs
autonomous: true
requirements: [AUTH-01, AUTH-04]
user_setup: []
must_haves:
  truths:
    - "A valid Telegram Mini App init-data string authenticates only its signed user."
    - "Tampered, expired, malformed, or tokenless init-data is rejected without account provisioning."
    - "A production-style missing webhook secret is rejected."
  artifacts:
    - path: lib/mmgo/telegram/web_app_auth.ex
      provides: Telegram Mini App initData HMAC validator
      min_lines: 80
      contains: "def authenticate"
    - path: test/mmgo/telegram/web_app_auth_test.exs
      provides: deterministic signature and expiry coverage
      min_lines: 80
  key_links:
    - from: lib/mmgo/telegram/web_app_auth.ex
      to: MMGO.Telegram configuration
      via: configured bot token and max authentication age
      pattern: "Application.get_env\\(:mmgo, MMGO.Telegram"
---

<objective>
Build the server-side Telegram Mini App verification primitive and make webhook-secret behavior safe by configuration.

Purpose: Account-owned browser sessions cannot exist until the server can prove that `initData` came from Telegram and reject stale/tampered data.
Output: A tagged-result `MMGO.Telegram.WebAppAuth` service, explicit runtime config, hardened webhook behavior, and deterministic tests.
</objective>

<execution_context>
@/Users/Albert/.codex/get-shit-done/workflows/execute-plan.md
@/Users/Albert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-player-identity/01-CONTEXT.md
@.planning/phases/01-secure-player-identity/01-RESEARCH.md
@lib/mmgo/telegram.ex
@lib/mmgo/accounts.ex
@config/runtime.exs
@config/test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish deterministic Mini App authentication tests</name>
  <files>test/mmgo/telegram/web_app_auth_test.exs</files>
  <read_first>config/test.exs, lib/mmgo/telegram.ex, test/mmgo/accounts_test.exs, https://core.telegram.org/bots/webapps#validating-data-received-via-the-mini-app</read_first>
  <action>Create `MMGO.Telegram.WebAppAuthTest` using `MMGO.DataCase` or a non-DB ExUnit case as appropriate. Add a private fixture that builds an init-data query from `%{"auth_date" => unix_seconds, "query_id" => "AAE...", "user" => Jason.encode!(telegram_user)}` by sorting `key=value` entries, calculating `secret_key = :crypto.mac(:hmac, :sha256, "test-bot-token", "WebAppData")`, calculating `Base.encode16(:crypto.mac(:hmac, :sha256, secret_key, data_check_string), case: :lower)`, and appending `hash`. Assert `WebAppAuth.authenticate(init_data, now: fixed_now)` returns normalized known user fields for valid data. Assert exact tagged failures for changed user/hash, missing hash, missing or malformed user, missing bot token, missing auth_date, and auth_date older than configured max age. Do not call external Telegram services or sleep.</action>
  <verify>mix test test/mmgo/telegram/web_app_auth_test.exs</verify>
  <acceptance_criteria>
    - test/mmgo/telegram/web_app_auth_test.exs contains `:crypto.mac(:hmac, :sha256` and tests valid, tampered, expired, missing-token, and malformed-user cases
    - `mix test test/mmgo/telegram/web_app_auth_test.exs` exits 0 after implementation
  </acceptance_criteria>
  <done>A test fixture proves the official HMAC data-check-string protocol without using a live credential.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded init-data validation and explicit Telegram configuration</name>
  <files>lib/mmgo/telegram/web_app_auth.ex, config/config.exs, config/runtime.exs, config/test.exs</files>
  <read_first>lib/mmgo/telegram.ex, lib/mmgo/accounts.ex, config/config.exs, config/runtime.exs, config/test.exs, test/mmgo/telegram/web_app_auth_test.exs</read_first>
  <action>Add one module `MMGO.Telegram.WebAppAuth` that exposes `authenticate(init_data, opts \\ [])`. It must: reject non-empty/non-binary input as `:missing_init_data`; obtain `:bot_token` and `:web_app_auth_max_age_seconds` from `Application.get_env(:mmgo, MMGO.Telegram, [])`; decode the raw query with `URI.decode_query/1`; remove `"hash"`; construct the alphabetically sorted newline-joined data-check string from all remaining raw fields; calculate the two HMAC-SHA-256 values documented in the research file; compare lower-case hex hashes with `Plug.Crypto.secure_compare/2` only when byte sizes match; parse `"auth_date"` as an integer and reject age below zero or above max; decode the `"user"` JSON with Jason; and return `{:ok, user_map}` only after all checks. Add `web_app_auth_max_age_seconds: 300` and an explicit `allow_insecure_webhook?: false` baseline configuration, set safe test values in `config/test.exs`, and have `runtime.exs` preserve a false production default while allowing an explicit non-production environment/config switch. Never write the raw init data or bot token to a log/flash.</action>
  <verify>mix test test/mmgo/telegram/web_app_auth_test.exs</verify>
  <acceptance_criteria>
    - lib/mmgo/telegram/web_app_auth.ex contains `def authenticate`, `URI.decode_query`, `Jason.decode`, `:crypto.mac`, and `Plug.Crypto.secure_compare`
    - config/config.exs or runtime config contains `web_app_auth_max_age_seconds: 300`
    - config/test.exs explicitly provides a bot token used by the auth tests
    - invalid input returns tagged errors instead of raising or creating account records
  </acceptance_criteria>
  <done>The application has a reusable, testable Mini App credential boundary with no new dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Harden webhook secret policy with focused regression coverage</name>
  <files>lib/mmgo/telegram.ex, test/mmgo/telegram_test.exs, test/mmgo_web/controllers/telegram_webhook_controller_test.exs</files>
  <read_first>lib/mmgo/telegram.ex, lib/mmgo_web/controllers/telegram_webhook_controller.ex, config/test.exs, test/mmgo_web/controllers/telegram_webhook_controller_test.exs</read_first>
  <action>Change `MMGO.Telegram.authorized_webhook_secret?/1` so a missing/empty configured secret returns true only when the explicit `allow_insecure_webhook?` configuration is true; otherwise it returns false. Preserve constant-time `Plug.Crypto.secure_compare/2` for equal-length configured secrets. Add direct unit coverage for explicit insecure local/test allowance, configured correct/wrong secret, and no-secret production-style false. Extend the webhook controller test to assert a request with no header receives 401 when the secret is absent and insecure allowance is false; retain the existing valid/invalid configured-secret checks. Restore any changed Application config in `on_exit/1` and keep affected tests serial if they mutate global config.</action>
  <verify>mix test test/mmgo/telegram_test.exs test/mmgo_web/controllers/telegram_webhook_controller_test.exs</verify>
  <acceptance_criteria>
    - lib/mmgo/telegram.ex contains `allow_insecure_webhook?`
    - a missing secret with no explicit allowance returns false in unit coverage
    - test/mmgo_web/controllers/telegram_webhook_controller_test.exs asserts a 401 response for the production-style missing-secret case
  </acceptance_criteria>
  <done>Webhook validation fails closed by default without breaking an explicitly configured local/test mode.</done>
</task>

</tasks>

<verification>
- [ ] `mix test test/mmgo/telegram/web_app_auth_test.exs test/mmgo/telegram_test.exs test/mmgo_web/controllers/telegram_webhook_controller_test.exs`
- [ ] `mix format --check-formatted`
- [ ] No auth test uses a live token, network request, or `Process.sleep/1`
</verification>

<success_criteria>
- HMAC validation follows the official `WebAppData` secret/data-check-string contract.
- Invalid Mini App data is safely rejected before provision code can run.
- Webhook behavior is fail-closed outside an explicit local/test allowance.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-player-identity/01-01-SUMMARY.md`.
</output>
