---
phase: 01-secure-player-identity
plan: "03"
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - lib/mmgo_web/controllers/telegram_auth_controller.ex
  - lib/mmgo_web/live/game_entry_live.ex
  - lib/mmgo_web/router.ex
  - assets/js/hooks/telegram-auth.js
  - assets/js/hooks/index.js
  - test/mmgo_web/controllers/telegram_auth_controller_test.exs
  - test/mmgo_web/live/game_entry_live_test.exs
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup: []
must_haves:
  truths:
    - "A verified Mini App request provisions/reuses its owner and redirects to the map with a scoped session."
    - "A normal browser sees a safe Telegram-entry state and does not receive a demo account automatically."
    - "Every routed game LiveView enters through the shared authorization mount."
  artifacts:
    - path: lib/mmgo_web/controllers/telegram_auth_controller.ex
      provides: CSRF-protected init-data to session endpoint
      min_lines: 60
      contains: "WebAppAuth.authenticate"
    - path: lib/mmgo_web/live/game_entry_live.ex
      provides: Russian Mini App authorization handoff UI
      min_lines: 80
      contains: "telegram-auth-form"
    - path: assets/js/hooks/telegram-auth.js
      provides: client handoff of Telegram.WebApp.initData
      min_lines: 30
  key_links:
    - from: lib/mmgo_web/controllers/telegram_auth_controller.ex
      to: lib/mmgo/telegram/web_app_auth.ex
      via: verified init-data authentication before Accounts provisioning
      pattern: "WebAppAuth\\.authenticate"
    - from: lib/mmgo_web/router.ex
      to: lib/mmgo_web/game_auth.ex
      via: authenticated game live_session on_mount
      pattern: "GameAuth"
---

<objective>
Deliver the real Mini App entry flow and route every game screen through the shared scope guard.

Purpose: A correct crypto primitive is not useful until the browser submits it, the server writes only verified session values, and game routes refuse unauthenticated connections.
Output: Telegram auth controller, mobile-first entry LiveView + hook, scoped router sessions, and focused controller/LiveView tests.
</objective>

<execution_context>
@/Users/Albert/.codex/get-shit-done/workflows/execute-plan.md
@/Users/Albert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-player-identity/01-CONTEXT.md
@.planning/phases/01-secure-player-identity/01-RESEARCH.md
@.planning/phases/01-secure-player-identity/01-UI-SPEC.md
@lib/mmgo_web/router.ex
@lib/mmgo_web/endpoint.ex
@assets/js/app.js
@assets/js/hooks/index.js
@lib/mmgo_web/controllers/play_demo_controller.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verified Telegram login controller and session tests</name>
  <files>lib/mmgo_web/controllers/telegram_auth_controller.ex, test/mmgo_web/controllers/telegram_auth_controller_test.exs</files>
  <read_first>lib/mmgo/telegram/web_app_auth.ex, lib/mmgo/accounts.ex, lib/mmgo_web/controllers/play_demo_controller.ex, lib/mmgo_web/router.ex, test/mmgo_web/controllers/telegram_webhook_controller_test.exs, test/mmgo/telegram/web_app_auth_test.exs</read_first>
  <action>Create `MMGOWeb.TelegramAuthController` with a browser `create/2` action. Read only `params["init_data"]`; call `MMGO.Telegram.WebAppAuth.authenticate/1`; then call `MMGO.Accounts.provision_from_telegram/1`; on success clear stale `:demo_character_id`/`:demo_opponent_id`, put `:current_account_id` and `:current_character_id` from the returned persisted records, and redirect to `~p"/map"`. Map all tagged validation/provision failures to a redirect back to `~p"/play"` with a generic Russian flash that leaks neither raw input nor secrets. Add controller tests with a real test HMAC fixture that assert account/character provisioning/reuse, only server-derived session IDs, tamper rejection without record creation, and no session mutation on failure.</action>
  <verify>mix test test/mmgo_web/controllers/telegram_auth_controller_test.exs test/mmgo/telegram/web_app_auth_test.exs</verify>
  <acceptance_criteria>
    - lib/mmgo_web/controllers/telegram_auth_controller.ex contains `WebAppAuth.authenticate` before `Accounts.provision_from_telegram`
    - successful controller test asserts `:current_account_id` and `:current_character_id` in session
    - invalid controller test asserts no account/identity/character record is created
  </acceptance_criteria>
  <done>The only production login endpoint writes scope IDs after cryptographic validation and provisioning succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Mini App entry contract with a bundled auth hook</name>
  <files>lib/mmgo_web/live/game_entry_live.ex, assets/js/hooks/telegram-auth.js, assets/js/hooks/index.js, test/mmgo_web/live/game_entry_live_test.exs</files>
  <read_first>.planning/phases/01-secure-player-identity/01-UI-SPEC.md, lib/mmgo_web/components/layouts.ex, lib/mmgo_web/live/travel_live.ex, assets/js/app.js, assets/js/hooks/index.js, AGENTS.md</read_first>
  <action>Create `MMGOWeb.GameEntryLive` at `/play`. Render `<Layouts.app flash={@flash} current_scope={@current_scope}>` and a full-height, dark mobile-first handoff with stable IDs `game-entry-screen`, `telegram-auth-root`, `telegram-auth-form`, and `telegram-auth-init-data`. Use `to_form/2` and `<.form method="post" action={~p"/auth/telegram"}>` with a hidden `<.input>`/input named `init_data`; include Russian loading/normal-browser/error copy exactly from `01-UI-SPEC.md`. Add `assets/js/hooks/telegram-auth.js` exporting `TelegramAuthHook`: on mount it reads `window.Telegram?.WebApp?.initData`, calls `ready()` when available, writes a non-empty string to the hidden field, and submits the form exactly once; when init data is missing it reveals the safe normal-browser state without a local demo fallback. Register only this hook in `assets/js/hooks/index.js`. Do not include external script tags, bot token, or inline raw JavaScript. Add LiveView tests for stable IDs/Russian normal-browser state and controller integration covers successful form destination.</action>
  <verify>mix test test/mmgo_web/live/game_entry_live_test.exs test/mmgo_web/controllers/telegram_auth_controller_test.exs</verify>
  <acceptance_criteria>
    - game_entry_live.ex contains `id="telegram-auth-form"`, `id="telegram-auth-root"`, and `action={~p"/auth/telegram"}`
    - assets/js/hooks/telegram-auth.js contains `window.Telegram?.WebApp?.initData` and does not contain a bot token literal
    - assets/js/hooks/index.js exports `TelegramAuth` in the Hooks registry
    - the entry page has no generic account/password form and no automatic demo redirect
  </acceptance_criteria>
  <done>Telegram launches submit raw init data through a CSRF-protected server form; normal browsers get a safe explanatory screen.</done>
</task>

<task type="auto">
  <name>Task 3: Route game pages through the current-scope live session</name>
  <files>lib/mmgo_web/router.ex</files>
  <read_first>lib/mmgo_web/router.ex, lib/mmgo_web/game_auth.ex, lib/mmgo_web/live/game_entry_live.ex, lib/mmgo_web/controllers/telegram_auth_controller.ex, AGENTS.md</read_first>
  <action>Keep `/`, `/play`, and `POST /auth/telegram` in the browser-public scope. Define one `live_session :game, on_mount: [{MMGOWeb.GameAuth, :require_character}]` containing `/map`, `/spellbook`, `/pvp`, `/screens`, all Academy, organisation, travel, party, combat, base, trade, inventory, alchemy, craft, finance, dungeon, event, and defeat LiveViews. Preserve development-only editor/dashboard routes outside or behind their appropriate development scope. Keep `/play/new`, `/play/continue`, `/demo/start`, and demo reset endpoints outside the production game session only until Plan 04 explicitly restricts them to local/demo configuration. Do not duplicate character-ID plugs per route.</action>
  <verify>mix test test/mmgo_web/live/game_auth_test.exs test/mmgo_web/live/game_entry_live_test.exs</verify>
  <acceptance_criteria>
    - lib/mmgo_web/router.ex contains `live_session :game` and `MMGOWeb.GameAuth`
    - every listed gameplay LiveView route is nested under that one `live_session`
    - `/play` and `POST /auth/telegram` remain reachable without an existing player scope
  </acceptance_criteria>
  <done>Game pages have one router-level authorization boundary while the entry/auth routes remain available to new Telegram users.</done>
</task>

</tasks>

<verification>
- [ ] `mix test test/mmgo_web/controllers/telegram_auth_controller_test.exs test/mmgo_web/live/game_entry_live_test.exs test/mmgo_web/live/game_auth_test.exs`
- [ ] `mix format --check-formatted`
- [ ] `git diff --check`
</verification>

<success_criteria>
- Production browser entry is a verified Mini App flow rather than a shared demo bootstrap.
- Auth/session failures have deliberate Russian UX and no privilege fallback.
- The router cannot mount a game screen without current scope.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-player-identity/01-03-SUMMARY.md`.
</output>
