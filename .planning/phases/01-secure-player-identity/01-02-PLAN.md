---
phase: 01-secure-player-identity
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mmgo/accounts.ex
  - lib/mmgo_web/game_auth.ex
  - test/mmgo/accounts_test.exs
  - test/mmgo_web/live/game_auth_test.exs
autonomous: true
requirements: [AUTH-02]
user_setup: []
must_haves:
  truths:
    - "A scope loads only a character owned by the account stored in the signed session."
    - "Missing, mismatched, inactive, or malformed session identifiers cannot mount a protected game route."
  artifacts:
    - path: lib/mmgo_web/game_auth.ex
      provides: current scope lookup and LiveView on_mount guard
      min_lines: 70
      contains: "def on_mount"
    - path: test/mmgo_web/live/game_auth_test.exs
      provides: scope authorization coverage
      min_lines: 70
  key_links:
    - from: lib/mmgo_web/game_auth.ex
      to: lib/mmgo/accounts.ex
      via: account-owned character query
      pattern: "Accounts\\.get.*character"
---

<objective>
Introduce one server-derived current-player scope and reusable LiveView authorization guard.

Purpose: Every later game command needs a single actor derived from the session rather than ad hoc `demo_character_id` or URL parameters.
Output: Account-owned character lookup, `MMGOWeb.GameAuth` scope/on-mount module, and focused authorization tests.
</objective>

<execution_context>
@/Users/Albert/.codex/get-shit-done/workflows/execute-plan.md
@/Users/Albert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-player-identity/01-CONTEXT.md
@.planning/phases/01-secure-player-identity/01-RESEARCH.md
@lib/mmgo/accounts.ex
@lib/mmgo/accounts/account.ex
@lib/mmgo/accounts/character.ex
@lib/mmgo_web.ex
@lib/mmgo_web/components/layouts.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an account-owned active-character query and test it</name>
  <files>lib/mmgo/accounts.ex, test/mmgo/accounts_test.exs</files>
  <read_first>lib/mmgo/accounts.ex, lib/mmgo/accounts/account.ex, lib/mmgo/accounts/character.ex, test/mmgo/accounts_test.exs</read_first>
  <action>Add a public Accounts function with a clear contract such as `get_active_character_for_account(account_id, character_id)` that accepts binary IDs, returns `{:ok, %Character{}}` only when the character belongs to that account and both account/character statuses allow gameplay, and returns a tagged `{:error, :not_found | :inactive}` result otherwise. Preload the account/current location only if the scope consumer needs them. Do not accept a character ID alone as authority. Add fixtures for two accounts/two characters plus tests for ownership match, cross-account rejection, unknown IDs, suspended account, and non-active character behavior.</action>
  <verify>mix test test/mmgo/accounts_test.exs</verify>
  <acceptance_criteria>
    - lib/mmgo/accounts.ex exports an account-and-character ownership lookup with both IDs in its public signature
    - test/mmgo/accounts_test.exs proves a character from account B is rejected for account A
    - no new function uses `Repo.get!(Character, character_id)` without ownership filtering for this scope path
  </acceptance_criteria>
  <done>A scope can obtain only an active character owned by the signed-in account.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reusable current scope and LiveView on-mount guard</name>
  <files>lib/mmgo_web/game_auth.ex, test/mmgo_web/live/game_auth_test.exs</files>
  <read_first>lib/mmgo_web.ex, lib/mmgo_web/router.ex, lib/mmgo_web/components/layouts.ex, lib/mmgo/accounts.ex, test/support/conn_case.ex</read_first>
  <action>Create `MMGOWeb.GameAuth` with a small scope map `%{account: account, character: character}` and `on_mount(:require_character, params, session, socket)`. Read only `"current_account_id"` and `"current_character_id"` from the LiveView session, call the new Accounts ownership lookup, and `assign(socket, :current_scope, scope)` on success. On missing, malformed, inactive, or mismatched session state, put a Russian error flash and return `{:halt, push_navigate(socket, to: ~p"/play")}`. Expose a non-LiveView helper only if controller code needs the same lookup; do not duplicate authorization rules. Add LiveView/connection tests that call the mount/route behavior with no session, valid scoped session, and deliberately cross-wired account/character session keys.</action>
  <verify>mix test test/mmgo_web/live/game_auth_test.exs test/mmgo/accounts_test.exs</verify>
  <acceptance_criteria>
    - lib/mmgo_web/game_auth.ex contains `def on_mount(:require_character` and assigns `:current_scope`
    - invalid scope paths navigate to `"/play"` rather than `/play/continue`
    - test/mmgo_web/live/game_auth_test.exs has explicit cross-account session coverage
  </acceptance_criteria>
  <done>One testable mount boundary protects game routes and gives LiveViews an account-owned current scope.</done>
</task>

</tasks>

<verification>
- [ ] `mix test test/mmgo/accounts_test.exs test/mmgo_web/live/game_auth_test.exs`
- [ ] `mix format --check-formatted`
</verification>

<success_criteria>
- Scope ownership is checked in the domain/account boundary, not just the router.
- A protected LiveView has a single reusable current-scope assign.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-player-identity/01-02-SUMMARY.md`.
</output>
